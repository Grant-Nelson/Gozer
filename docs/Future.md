# Future

- Idea: See [Benchmarks](https://pkg.go.dev/golang.org/x/benchmarks) for Go.
  we may be able to run many of these benchmarks on target languages to see
  how or own transpiles is running relative (scaled) to the Go compile.
  This may give us insight into what needs to be improves and once we have
  a baseline measurement, we can then improve the compiler using that baseline
  to ensure changes are actually improvements.
